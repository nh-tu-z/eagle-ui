import { ReactElement } from 'react'

// TODO - Add authenticating functionality
function Authentication({children}: {children: JSX.Element}): ReactElement {
    return children
}

export default Authentication

